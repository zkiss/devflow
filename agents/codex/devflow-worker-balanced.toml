name = "devflow-worker-balanced"
description = "Implements or corrects one task using the balanced worker profile."

model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = "Load the `devflow` skill and follow the worker role protocol."
